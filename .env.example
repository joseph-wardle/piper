# piper — environment variable overrides
#
# Copy this file to .env and adjust values for your environment.
# All variables are optional; defaults are declared in conf/settings.toml.
# Never commit a populated .env file.

# Path to an alternate config file (TOML, same shape as conf/settings.toml).
# PIPER_CONFIG_FILE=/path/to/custom_settings.toml

# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------

# Root of the shared JSONL spool written by the sandwich pipeline.
# PIPER_PATHS_RAW_ROOT=/groups/sandwich/05_production/.telemetry/raw

# Parent directory for all piper-managed output (warehouse, silver, state…).
# PIPER_PATHS_DATA_ROOT=/groups/sandwich/05_production/.telemetry

# ---------------------------------------------------------------------------
# Ingestion
# ---------------------------------------------------------------------------

# Files modified more recently than this (seconds) are skipped on each pass.
# Raise if the pipeline flushes very large files that take longer to close.
# PIPER_INGEST_SETTLE_SECONDS=120

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------

# Log verbosity: DEBUG | INFO | WARNING | ERROR
# PIPER_LOGGING_LEVEL=INFO

# Log format: "json" (structured, for aggregators) | "text" (human-readable)
# PIPER_LOGGING_FORMAT=json

# ---------------------------------------------------------------------------
# Privacy
# ---------------------------------------------------------------------------

# Set to "true" to hash host_user in gold models before Grafana sees it.
# PIPER_PRIVACY_MASK_USERS=false
