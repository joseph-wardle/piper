name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run ty check
      - run: uv run pytest
      - name: Studio tests with Maya's USD
        run: uv run --with usd-core==24.11 python -m pytest packages/piper-studio
      - name: Core imports with no third-party packages
        run: >
          uv run --isolated --no-project --with ./packages/piper-core
          python -c "import piper, piper.errors, piper.find, piper.registry, piper.tracker"
      - run: uv build --all-packages
