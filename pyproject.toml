[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
piper-core = { workspace = true }
piper-maya = { workspace = true }
piper-shotgrid = { workspace = true }
piper-studio = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=9.1",
    "ruff>=0.14.2",
    "ty==0.0.79",
]

[tool.ty.analysis]
# USD's and Maya's Python modules are compiled extensions with no type
# information, and Maya's, its Qt among them, are only ever present inside Maya.
replace-imports-with-any = ["pxr", "pxr.**", "maya", "maya.**", "PySide6"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "SIM", "ANN", "PTH", "RUF"]

[tool.pytest.ini_options]
testpaths = ["packages"]
addopts = ["-m", "not integration"]
markers = [
    "integration: talks to a real external system; needs credentials",
]

[tool.ruff.lint.isort]
known-first-party = ["piper", "piper_cli", "piper_maya", "piper_shotgrid", "piper_studio"]
