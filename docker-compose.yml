# Piper — Grafana visualisation stack
#
# Usage:
#   scripts/setup_grafana_plugin.sh   # once — downloads the DuckDB plugin
#   podman compose up -d
#
# The DuckDB datasource opens the database in a mode that requires write access.
# Mount the whole warehouse directory so DuckDB can create sidecar state files.
# Set PIPER_DATA_ROOT to override the default telemetry root, e.g.:
#   PIPER_DATA_ROOT=/tmp/piper_dev podman compose up -d
#
# Default credentials: admin / piper  (change via GF_SECURITY_ADMIN_PASSWORD)

services:
  grafana:
    # The MotherDuck DuckDB plugin requires the Ubuntu/glibc Grafana image.
    image: grafana/grafana:12.4.1-ubuntu
    user: "0"
    ports:
      - "3000:3000"
    environment:
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: "motherduck-duckdb-datasource"
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: piper
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: /etc/grafana/provisioning/dashboards/data_quality.json
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/plugins/motherduck-duckdb-datasource:/var/lib/grafana/plugins/motherduck-duckdb-datasource:ro
      - ${PIPER_DATA_ROOT:-/groups/sandwich/05_production/.telemetry}/warehouse:/var/piper
      - grafana_data:/var/lib/grafana
    restart: unless-stopped

volumes:
  grafana_data:
