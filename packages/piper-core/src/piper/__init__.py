"""Production gateway operations shared by every Piper presentation."""

# Hard-coded rather than read from distribution metadata: this package is
# deployed into Maya and Houdini over PYTHONPATH, where no dist-info exists.
__version__ = "0.1.0"
