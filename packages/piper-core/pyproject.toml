[project]
name = "piper-core"
version = "0.1.0"
description = "Production gateway operations shared by every Piper presentation."
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "piper"
