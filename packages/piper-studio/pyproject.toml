[project]
name = "piper-studio"
version = "0.1.0"
description = "Studio conventions, production configuration, and provider selection."
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "piper-core",
    "piper-shotgrid",
]

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
